import {avatar} from "../icons/icons"

interface BookClubProps{
    
}

export default function BookClub(props: BookClubProps) {

    return (
      <div className="p-10">
       book Club
      </div>
    )
  }